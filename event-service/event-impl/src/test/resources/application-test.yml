spring:
  datasource:
    url: jdbc:tc:postgresql:15.12:///event-service-db
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: local
    password: locallocal
    hikari:
      maximum-pool-size: 2
  cloud:
    consul:
      enabled: false
  liquibase:
    change-log: classpath:db/changelog/changelog-master.xml

yandex:
  s3:


yoomoney:
